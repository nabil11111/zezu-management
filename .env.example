# Postgres connection string (Railway).
DATABASE_URL=postgres://user:password@host:5432/railway

# Random secret used to sign the session cookie AND to hash the 4-digit
# access codes (HMAC-SHA256). Changing it invalidates every session and
# every stored code. Generate one with:
#   openssl rand -base64 48
SESSION_SECRET=changeme
